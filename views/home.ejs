<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Home</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #6c63ff, #48c6ef);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .card {
      border: none;
      border-radius: 20px;
      background-color: #fff;
    }

    .card h3 {
      color: #6c63ff;
      font-weight: 600;
    }

    .btn-logout {
      background-color: #ff4b5c;
      border: none;
    }

    .btn-logout:hover {
      background-color: #e8414e;
    }

    .navbar {
      background-color: #ffffffcc;
      backdrop-filter: blur(8px);
    }

    .navbar-brand {
      font-weight: 600;
      color: #6c63ff;
    }

    .email-text {
      font-size: 0.85rem;
      color: #555;
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light shadow-sm px-4">
    <div class="container-fluid d-flex justify-content-between">
      <div>
        <span class="navbar-brand"><%= username %></span>
        <span class="email-text">(<%= email %>)</span>
      </div>
      <div>
        <a href="/logout" class="btn btn-logout text-white px-4 rounded-pill">Sign Out</a>
      </div>
    </div>
  </nav>

  <!-- Center Card -->
  <div class="d-flex align-items-center justify-content-center vh-100">
    <div class="card p-5 shadow-lg text-center" style="width: 400px; margin-top: -100px;">
      <div class="mb-4">
        <img src="https://cdn-icons-png.flaticon.com/512/847/847969.png" width="80" alt="User Icon">
      </div>
      <h3>Welcome, <%= username %>!</h3>
      <p class="text-muted">You are now logged in.</p>
    </div>
  </div>

</body>
</html>
